{
  "openapi": "3.1.0",
  "info": {
    "title": "Alpheus AP0 Common Contract Profile",
    "version": "1.0.0",
    "description": "AP0-2 exposes no HTTP endpoint. Later APIs reference the frozen common schemas."
  },
  "paths": {},
  "components": {
    "schemas": {
      "CommandEnvelope": {"$ref": "../schema/common.schema.json#/$defs/CommandEnvelope"},
      "Failure": {"$ref": "../schema/common.schema.json#/$defs/Failure"},
      "RunOrigin": {"$ref": "../schema/common.schema.json#/$defs/RunOrigin"}
    }
  }
}
