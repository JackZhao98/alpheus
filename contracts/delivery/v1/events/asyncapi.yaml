{
  "asyncapi": "3.0.0",
  "info": {"title": "Alpheus AP0 Durable Delivery", "version": "1.0.0", "description": "Concrete channels are owner-pack declarations; AP0 freezes the durable transport records."},
  "channels": {},
  "components": {"schemas": {"OutboxRecord": {"$ref": "../schema/delivery.schema.json#/$defs/OutboxRecord"}}}
}
